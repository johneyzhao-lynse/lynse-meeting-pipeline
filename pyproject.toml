[project]
name = "lynse-meeting-pipeline"
version = "0.1.0"
description = "CLI pipeline for structured meeting summaries from audio transcripts"
requires-python = ">=3.10"

[project.scripts]
lynclaw = "tools.lynclaw_agent:main"
lynse = "tools.lynse:main"
